# URL del backend. En desarrollo local, si no se define, usa http://localhost:4000
# En producción (Vercel), configúrala en las variables de entorno del proyecto
# apuntando a la URL del backend desplegado (ej. Railway).
VITE_API_URL=http://localhost:4000
